// Capture the tab, pass to taps list to find a tap.
//

function taps() {

  
  function checkDefinedTaps(tap) {
    // Send the tap key into the definedTaps list.
    // Need to work with JL to figure out what this tap looks like.
    // Run callback attached to tap to perform action.
    definedTaps.[tap];
  }

}


module.exports = taps;
